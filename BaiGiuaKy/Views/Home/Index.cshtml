@model X.PagedList.IPagedList<BaiGiuaKy.Models.Product>

@section Scripts
{
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script>
        $(function () {
            $("#autocomplete").autocomplete({
                source: '/Home/AutocompleteSearch'
            });
        });
    </script>
}

@{
    ViewData["Title"] = "Trang Chủ";
}

@using (Html.BeginForm("Search", "Home", FormMethod.Get))
{
    <div class="header_bottom d-flex justify-content-end align-items-center">
        <div class="input-group input-group-sm">
            <input type="text" id="autocomplete" name="searchString" class="form-control rounded-end" style="width: 200px;" placeholder="Tìm kiếm" aria-label="Tìm kiếm" aria-describedby="search-addon">
            <button type="submit" class="btn btn-outline-primary rounded-start"><i class="bi bi-search"></i></button>
        </div>
        <a href="@Url.Action("Index", "ShoppingCart")" class="btn btn-outline-light ms-3">
            <i class="bi-cart-fill me-1"></i>
            Giỏ hàng
            <span class="badge bg-secondary">@ViewBag.CartItemCount</span>
        </a>
    </div>
}





<div class="header_slide">
    <div class="header_bottom_left">
        <div class="categories">
            <ul>
                <h3>Danh mục sản phẩm</h3>
                <li><a href="@Url.Action("ShowProductsByCategory", "Home", new { categoryId = 1 })">Loại 1</a></li>
                <li><a href="@Url.Action("ShowProductsByCategory", "Home", new { categoryId = 2 })">Loại 2</a></li>
                <li><a href="@Url.Action("ShowProductsByCategory", "Home", new { categoryId = 3 })">Loại 3</a></li>
                <li><a href="@Url.Action("ShowProductsByCategory", "Home", new { categoryId = 4 })">Loại 4</a></li>
                <li><a href="@Url.Action("ShowProductsByCategory", "Home", new { categoryId = 5 })">Loại 5</a></li>
            </ul>
        </div>
    </div>
    <div class="header_bottom_right">
        <div class="slider">
            <div id="slider">
                <div id="mover">
                    <div id="slide-1" class="slide">
                        
                        <div class="slider-text">
                            <h1>BùNG NỔ<br><span>TƯNG BỪNG</span></h1>
                            <h2>Giá cả phải chăng</h2>
                            <div class="features_list">
                                <h4>Mua ngay nào!</h4>
                            </div>
                            <a href="/home" class="button">Mua Ngay</a>
                        </div>
                        <div class="slider-img">
                            <a href="/home"><img src="~/images/slide-3-image.jpg" alt="learn more" /></a>
                        </div>
                        <div class="clear"></div>
                    </div>
                    @* <div class="slide">
                        <div class="slider-text">
                            <h1>Clearance<br><span>SALE</span></h1>
                            <h2>UPTo 40% OFF</h2>
                            <div class="features_list">
                                <h4>Get to Know More About Our Memorable Services</h4>
                            </div>
                            <a href="preview.html" class="button">Shop Now</a>
                        </div>
                        <div class="slider-img">
                            <a href="preview.html"><img src="~/images/slide-3-image.jpg" alt="learn more" /></a>
                        </div>
                        <div class="clear"></div>
                    </div>
                    <div class="slide">
                        <div class="slider-img">
                            <a href="preview.html"><img src="~/images/slide-2-image.jpg" alt="learn more" /></a>
                        </div>
                        <div class="slider-text">
                            <h1>Clearance<br><span>SALE</span></h1>
                            <h2>UPTo 10% OFF</h2>
                            <div class="features_list">
                                <h4>Get to Know More About Our Memorable Services Lorem Ipsum is simply dummy text</h4>
                            </div>
                            <a href="preview.html" class="button">Shop Now</a>
                        </div>
                        <div class="clear"></div>
                    </div> *@
                </div>
            </div>
            <div class="clear"></div>
        </div>
    </div>
    <div class="clear"></div>
</div>

<h1 class="text-center my-4">Sản Phẩm</h1>
<div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-4">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="card h-100 shadow-sm product-card" onclick="redirectToProductDetail('@item.Id')">
                <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name" style="height: 200px; object-fit: cover;">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="text-primary fw-bold">@item.Price.ToString("#,##0") VND</span>
                        <a asp-controller="ShoppingCart" asp-action="AddToCart" asp-route-ProductId="@item.Id" asp-route-quantity="1" class="btn btn-outline-primary">
                            <i class="bi-cart-plus-fill"></i> Thêm vào giỏ
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

<script>
    function redirectToProductDetail(productId) {
        window.location.href = "/Home/Display?id=" + productId;
    }
</script>

<div class="container mt-4">
    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center">
            @if (Model.HasPreviousPage)
            {
                <li class="page-item">
                    <a class="page-link" href="?page=1">Đầu</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="?page=@(Model.PageNumber - 1)">Trước</a>
                </li>
            }
            @for (int i = 1; i <= Model.PageCount; i++)
            {
                <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                    <a class="page-link" href="?page=@i">@i</a>
                </li>
            }
            @if (Model.HasNextPage)
            {
                <li class="page-item">
                    <a class="page-link" href="?page=@(Model.PageNumber + 1)">Tiếp</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="?page=@Model.PageCount">Cuối</a>
                </li>
            }
        </ul>
    </nav>
</div>
