@model BaiGiuaKy.Models.Product

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title">@Model.Name</h3>
                </div>
                <div class="card-body">
                    <div class="text-center">
                        @if (!string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <img src="@Model.ImageUrl" alt="Product Image" class="img-fluid rounded" style="max-height: 300px;" />
                        }
                    </div>
                    <h4 class="text-muted mt-3">Giá: <span class="text-danger">@Model.Price </span></h4>
                    <h4 class="text-muted mt-3">Loại SP: <span class="text-danger">@Model.Category.Name </span></h4>
                    <p class="product-description mt-3">Thông tin chi tiết: @Model.Description</p>
                    <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
                </div>
            </div>
        </div>
    </div>
</div>
